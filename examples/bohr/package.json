{
	"name": "bohr",
	"version": "0.0.2",
	"private": true,
	"description": "The not quite bohr model of the atom",
	"main": "bundle.js",
	"scripts": {
		"build": "webpack --mode=production --progress --colors",
		"dev": "webpack --mode=development --progress --colors",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"author": "Kevin Doughty",
	"license": "Apache-2.0",
	"dependencies": {
		"hyperact": "^0.0.18"
	},
	"devDependencies": {
		"babel-core": "^6.26.3",
		"babel-loader": "^7.1.5",
		"babel-preset-env": "^1.7.0",
		"webpack": "^4.16.2",
		"webpack-cli": "^3.1.0",
		"webpack-dev-server": "^3.1.5"
	}
}
